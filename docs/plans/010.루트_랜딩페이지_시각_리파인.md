# 010. 루트 랜딩페이지 시각 리파인 (Round 2)

## Context

008 작업(만차로 → 화물맨 리브랜딩 + 배너 카드 레이아웃)에 이어, 사용자가 제공한 마케팅 시안에 시각적으로 더 가깝게 다듬기.

**의사결정**
1. 트럭 SVG 일러스트 → 실사 톤 PNG 이미지로 교체 (사용자 제공)
2. 5단계 이모지 아이콘 → 시안에서 잘라낸 파란 원형 PNG로 교체
3. `--primary` 컬러를 오렌지 → 네이비 블루로 전체 전환 (전체 사이트 영향)

## 변경 대상 파일

### 수정
- `src/app/globals.css` — `--primary`를 `oklch(0.32 0.18 264)` (네이비 블루 ~#1e3a8a)로 변경
- `src/app/(customer)/page.tsx` — `next/image` import 추가, `WIZARD_STEPS` 배열 구조 변경(`icon` 이모지 → `img` 경로), 5단계 렌더링을 `<Image fill object-cover>`로 교체(이중 원 방지를 위해 `bg-primary` 제거), 우측 `<HeroTruckIllustration />` → `<Image src="/landing/hero-truck.png" fill priority>`로 교체

### 삭제
- `src/components/brand/HeroTruckIllustration.tsx`

### 신규 자산 (사용자 제공 원본에서 가공)
- `public/landing/hero-truck.png` — `C:\Users\user\Downloads\hmm_logo.png` 그대로 복사
- `public/landing/step-01-home.png` ~ `step-05-vehicle.png` — `C:\Users\user\Downloads\moving_icon.png`에서 하단 5개 원형 아이콘 영역만 PowerShell + System.Drawing으로 크롭 (각 220×220px, y=660 기준, x 간격 269px)

## 구현 단계

1. globals.css `--primary` 값 교체
2. `next/image` import 추가, `WIZARD_STEPS` 구조 변경
3. 우측 트럭 영역: `<HeroTruckIllustration />` → `<Image>` 교체
4. `HeroTruckIllustration.tsx` 삭제
5. PowerShell로 자산 가공 (`Copy-Item` + `Bitmap.Clone`)
6. 이중 원 방지: `<div>` 컨테이너의 `bg-primary` 제거, `Image` 옵션을 `fill + object-cover`로 변경
7. tsc 타입 체크 + dev 서버 시각 확인

## 좌표 산출 근거 (5단계 크롭)
원본 `moving_icon.png` 1536×1024. 5개 원형이 균등 분포, 각 지름 약 220px:
- 1차(280px 간격) 시도 → 4·5번 어긋남
- 2차(269px 간격, x=120/389/658/927/1196, y=660) → 정상

## Out of scope
- `MoveType` 스키마 (학생이사 추가 없음)
- 페이지 레이아웃/카피 변경
- 다른 페이지 마크업 (컬러만 자동 네이비 전환)
- DB, Supabase, 환경변수

## Verification
- `npx tsc --noEmit` 통과
- `npm run dev` + 브라우저로 / 진입, 시안과 시각적 흡사도 확인
- 5단계 아이콘이 잘림/이중 원 없이 깔끔하게 노출
- 트럭 이미지가 우측에 자연스럽게 배치
- `/landing`, `/estimate` 등 다른 페이지의 primary 컬러도 네이비로 자동 전환

## 후속 (실행 이후)
- 본 계획 사본 → 이 파일
- 결과 → `docs/worklogs/010.루트_랜딩페이지_시각_리파인.md`
- git push는 사용자 확인 후 진행 (008 작업과 함께 묶을지 분리할지 미정)
